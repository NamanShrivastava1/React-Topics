// import MovieCard from "../components/MovieCard";

// const Favorites = () => {

//   return (
//     <main className="mx-auto max-w-6xl px-5 py-10">
//       <h1 className="text-3xl font-bold tracking-tight">Your favorites</h1>
//       {favorites.length === 0 ? (
//         <div className="mt-12 text-center text-stone-500">
//           <h2 className="text-lg font-semibold text-stone-800">No favorite movies yet</h2>
//           <p className="mt-2">Tap the heart on a movie to save it here.</p>
//         </div>
//       ) : (
//         <div className="mt-8 grid grid-cols-2 gap-4 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5">
//           {favorites.map((movie) => <MovieCard key={movie.id} movie={movie} />)}
//         </div>
//       )}
//     </main>
//   );
// };

// export default Favorites;


const Favourites = () => {
  return (
    <div>Favourites</div>
  )
}

export default Favourites
